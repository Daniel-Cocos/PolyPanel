# GreenTech
GreenTech backend runs locally with `docker compose up --build`. The frontend is deployed separately from `frontend/` with `npm run deploy`, which publishes only `frontend/dist` to the `gh-pages` branch for GitHub Pages hosting.

## Website

[https://daniel-cocos.github.io/GreenTech/](https://daniel-cocos.github.io/GreenTech/)
